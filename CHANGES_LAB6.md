# Порядок змін для додавання функціоналу лікарів та фільтрації (lab 6, крок 9)

## Крок 1: Оновлення моделі Doctor
- Додано поле `rating` (DecimalField для рейтингу від 0.0 до 5.0)
- Додано поле `procedures` (ManyToManyField до Service для процедур, які проводить лікар)
- Створено міграцію `0005_doctor_procedures_doctor_rating.py`
- Застосовано міграцію до бази даних

## Крок 2: Оновлення views.py
- Додано імпорт Doctor
- Оновлено `home` для передачі топ 3 лікарів на головну сторінку
- Оновлено `service_detail` для передачі лікарів, які проводять цю послугу (`doctors = Doctor.objects.filter(procedures=service)`)
- Додано нову view `doctors` з фільтрацією за рейтингом (діапазон від цілого числа до наступного, наприклад rating=2 показує 2.0-2.999)

## Крок 3: Оновлення urls.py
- Додано URL для `doctors` view: `path('doctors/', views.doctors, name='doctors')`

## Крок 4: Оновлення admin.py
- Додано `rating` до `list_display` у DoctorAdmin
- Додано `rating` до `list_filter` для фільтрації за рейтингом

## Крок 5: Оновлення base.html
- Додано dropdown меню "Лікарів" у навігації
- Додано посилання для фільтрації за рейтингом (Рейтинг 1, Рейтинг 2, ..., Рейтинг 5)

## Крок 6: Оновлення service_detail.html
- Додано блок вибору лікаря (select) для послуги
- Кнопка "Записатися" активується тільки після вибору лікаря (JavaScript)
- Якщо лікарів немає, показується повідомлення

## Крок 7: Створення doctors.html
- Новий шаблон для відображення списку лікарів
- Відображає ім'я, спеціалізацію, рейтинг, коефіцієнт ціни, опис, контактні дані, процедури

## Крок 8: Оновлення style.css
- Додано стилі для `.doctor-card` аналогічно `.service-card`

## Крок 9: Створення fixture doctors.json
- Додано 11 лікарів з різними рейтингами (від 1.9 до 5.0) та процедурами
- Завантажено fixture до бази даних

## Крок 10: Додавання коефіцієнта ціни
- У doctors.html додано текстовий коефіцієнт залежно від рейтингу:
  - rating == 3.0: коефіцієнт 1.0 (ціна така сама)
  - rating > 3.0: коефіцієнт 1.1 (ціна зростає)
  - rating < 3.0: коефіцієнт 0.9 (ціна зменшується)

## Крок 11: Додавання коментарів
- Додано коментарі до всіх файлів з описом змін для lab 6, крок 9